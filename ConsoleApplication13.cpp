#include <iostream>
#include <cmath>
#include <math.h>

using namespace std;

int main() {
	double x;
	cout << "Enter the value of x: ";
	cin >> x;


	 double y = tan(abs(2 * pow(x, 2) + 5 * x) - log10((x - 2, 5)) / log10(5)) / sqrt(abs(pow(sin(x), 4) * pow(x, 3) + log10((pow(x, 2) - 2, 5)) / log10(5)));

	cout << "y = " << y << endl;

	return 0;
}